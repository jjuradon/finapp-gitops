# Contributing to gitops

## Repo purpose
Orchestrates the local stack, CI, Nginx, and documents architecture.

## Running locally
Use `make up` to run the stack.

## Adding a new feature
Reference agent_feature_workflow.md.

## Pre-commit setup
Install pre-commit hooks.

## Tests
Run `make ci-local`.

## Architectural Context
See `docs/ADR/`.
